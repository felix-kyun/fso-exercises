mutation addBook(
  $title: String!
  $genres: [String!]!
  $author: String!
  $published: Int!
) {
  addBook(
    title: $title
    genres: $genres
    author: $author
    published: $published
  ) {
    __typename
  }
}
